#include <stdio.h>

int main() {
    printf("Hello, World!\n");

    // Pause so the terminal stays open
    printf("Press Enter to exit...");
    getchar();  // Wait for Enter key

    return 0;
}
